<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tvstorm.tv.siservice"
    android:sharedUserId="android.uid.system"
    android:versionCode="20242000"
    android:versionName="2.2.42" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SET_TIME" />
    <uses-permission android:name="android.permission.SET_TIME_ZONE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:persistent="true"
        android:theme="@style/AppTheme" >

        <uses-library android:name="tvcontextmanager" android:required="false" />
        <meta-data android:name="LKN_TV_SYSTEM_APP" android:value="true" />

        <service android:name=".TVSIService" >
            <intent-filter>
                <action android:name="com.tvstorm.tv.siservice" />
            </intent-filter>
        </service>

        <receiver
            android:name=".TVSIServiceReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.tvstorm.tv.SLEEP_WAKEUP" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.tvstorm.tv.HIDDEN_MENU" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.tvstorm.tv.systemframework.network.NetStateChangedEvent" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
